# Corpus analysis (RQ1-RQ3). The RQ4 experiment's scanner versions are pinned
# separately in the Dockerfile and listed in requirements-rq4.txt.
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scipy>=1.10
scikit-learn>=1.3
requests>=2.28
